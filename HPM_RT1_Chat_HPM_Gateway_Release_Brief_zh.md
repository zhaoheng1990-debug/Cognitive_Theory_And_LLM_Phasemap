# HPM-RT1 Chat HPM Gateway Release Brief

版本号：`v1.8.6`

## 本次版本定位

`v1.8.6` 是一次交付收口版，重点不是继续扩理论面，而是把已经完成的 Chat HPM Gateway 能力完整装进可迁移安装包，并补齐用户从安装到激活到接入审计模型的全流程引导。

## 本次收口重点

1. 新增在线模型输入侧审计接入
2. 新增在线模型输出侧后验审计接入
3. 审计台支持在线 API 方式做输入+输出双侧审计
4. `runtime.env.example` 纳入交付包
5. 浏览器插件版本同步升级到 `1.8.6`
6. 新增完整安装与激活总 guide

## 用户价值

用户现在可以：

- 安装后快速启动 Gateway 和审计台
- 激活浏览器插件进行网页对话审计
- 通过在线模型 API 完成输入侧和输出侧审计
- 通过本地模型完成输入侧审计
- 使用 `/v1/precheck`、`/v1/postaudit`、`/v1/gateway` 完成 API 集成

## 仍然保留的边界

当前版本仍是：

```text
黑盒外 AI Trust Gateway / Advisory Runtime
```

不读取在线大模型内部推理参数，不自动改写回答，只负责预警、审计、日志与建议动作。
