# PM Correction: Insert Autonomous ICM Evolution Policy Before Release

## Verdict

The user's correction is accepted.

AgentOS cannot remain candidate-only or human-preauthorized-only for ICM / MemoryUnit / OperatorMemory evolution.

If a research line such as UPS closes to ACCEPT with sufficient evidence, AgentOS should be able to autonomously update its project-scoped durable ICM / OperatorMemory under Kernel-owned policy.

\[
\boxed{
Self\text{-}evolution\ requires\ autonomous,\ policy\text{-}governed\ retention.
}
\]

## Why this is not a new side branch

This is not adding a new capability family.

It closes a core AGI-preccursor gap:

```text
Selection
→ Retention
→ ICMUpdate
→ FutureReuse
```

Without this, AgentOS can evaluate and recommend but cannot self-evolve.

## Critical correction

Previous controlled write line proved:

```text
controlled local sandbox write
```

But release-preflight now needs:

```text
project-scoped durable ICM evolution write
```

under strict Kernel-owned policy.

## Core principle

\[
\boxed{
LLM\ API\ advises;\ Harness\ executes;\ AgentOSKernel\ evolves.
}
\]

Human review remains important, but not as a per-write precondition for ordinary project-scoped self-evolution.

Human authorization is required only for:

- production/global scope escalation;
- external mutation;
- high-risk domain decisions;
- user-visible irreversible actions;
- legal / medical / financial / investment binding actions.
